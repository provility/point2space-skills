{
  "expression": "decomposeperp",
  "_requires": ["g2d", "vector"],
  "description": "Decomposes a vector into its component perpendicular to a reference vector.",
  "utility": "Perpendicular component of a vector",
  "arguments": [
    {"name": "graph", "type": "g2d", "required": true, "description": "2D graph container"},
    {"name": "vector", "type": "vector variable", "required": true, "description": "Vector to decompose"},
    {"name": "refVector", "type": "vector variable", "required": true, "description": "Reference direction vector"}
  ],
  "examples": [
    {"scenario": "Perpendicular decomposition", "expression": "G = g2d(at(4,4), size(20), type(geometry))\nV = vector(G, (0,0), (3,4), c(black))\nR = vector(G, (0,0), (5,0), c(gray))\nVn = decomposeperp(G, V, R, c(blue))"}
  ]
}
