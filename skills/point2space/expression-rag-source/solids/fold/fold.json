{
  "expression": "fold",
  "_requires": ["cut"],
  "description": "Creates a 3D folded shape from a cut() sheet expression. Converts a 2D net/sheet into its 3D form.",
  "utility": "Fold a 2D net into 3D",
  "arguments": [
    {"name": "cut", "type": "cut variable", "required": true, "description": "Reference to a cut() expression"},
    {"name": "position", "type": "at(row, col)", "required": false, "description": "Canvas position"},
    {"name": "viewSize", "type": "view(h, w)", "required": false, "description": "Container size"},
    {"name": "color", "type": "c(color)", "required": false, "description": "Edge color"},
    {"name": "fillColor", "type": "fc(color)", "required": false, "description": "Fill color"},
    {"name": "fillOpacity", "type": "fo(number)", "required": false, "description": "Fill opacity"}
  ],
  "examples": [
    {"scenario": "Fold a cube net", "expression": "S = sheetrect(at(4,4), view(10,10), 3, 4)\nC = cut(S, cuttype(cross))\nF = fold(C, at(4,16), view(12,12))"}
  ]
}
